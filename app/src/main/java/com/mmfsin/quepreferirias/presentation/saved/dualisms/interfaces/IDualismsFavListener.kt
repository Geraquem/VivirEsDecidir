package com.mmfsin.quepreferirias.presentation.saved.dualisms.interfaces

interface IDualismsFavListener {
    fun onDualismFavClick(dualismId: String)
    fun onDualismFavLongClick(dualismId: String)
}